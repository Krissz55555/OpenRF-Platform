# MQTT Core v0.3.0

The bridge keeps its setup AP and optionally connects to a WiFi network in AP+STA mode. MQTT reconnect is non-blocking. Any payload sent to `openrf/<device>/slot/<n>/send` triggers that slot. Home Assistant Discovery is intentionally reserved for v0.3.1.
